<%= form_for(description) do |f| %>
  <% if description.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(description.errors.count, "error") %> prohibited this description from being saved:</h2>

      <ul>
      <% description.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :category_id %>
   <!-- <%= f.number_field :category_id %> -->
   <%=   collection_select :description, :category_id, Category.all, :id, :name,  prompt: false %>
   </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
