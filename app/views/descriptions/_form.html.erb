<%= form_for(description) do |f| %>
  <% if description.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(description.errors.count, "error") %> prohibited this description from being saved:</h2>

      <ul>
      <% description.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :DescriptionName %>
    <%= f.text_field :DescriptionName %>
  </div>

  <div class="field">
    <%= f.label :Categories_id %>
    <%= f.number_field :Categories_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
