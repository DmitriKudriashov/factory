<%= form_for(usingtype) do |f| %>
  <% if usingtype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(usingtype.errors.count, "error") %> prohibited this usingtype from being saved:</h2>

      <ul>
      <% usingtype.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
