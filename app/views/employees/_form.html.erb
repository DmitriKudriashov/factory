<%= form_for(employee) do |f| %>
  <% if employee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(employee.errors.count, "error") %> prohibited this employee from being saved:</h2>

      <ul>
      <% employee.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    Employee:
    <%= f.text_field :name %>
  </div>

  <div class="field">
    Position: 
    <%=     collection_select :employee, :position_id, Position.all.order(:name), :id, :name,  prompt: false %>
   </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
